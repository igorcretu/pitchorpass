# Local development — Vite proxy forwards /api to localhost:8000
# VITE_API_URL is intentionally unset here so the proxy kicks in
# For production, set this in Netlify env vars or GitHub secrets
VITE_API_URL=
